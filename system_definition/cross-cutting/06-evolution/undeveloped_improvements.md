
---

Change methodology where details of a task is passed between agents in files and only minimal information is passed agent to agent  

--- 

Track a task in the file structure to enable continuous task evolvement
This should be applicable for all tasks and agents to enable tracking details and status of a task 
currently only plan is tracked, extend this to tasks and agents that would benefit and create traceability

---

To minimize content rot over time it is favorable to outsource smaller tasks to sub-agents

Encurage agents to split task into multiple smaller tasks if possible and outsource them to helpers
This would enable the planner to divide and conquer larger tasks without experiencing context rot in itself and downstream builders

Multiple tasks can be stored as files that each subtask can get access to to not pass infinite information thr the router