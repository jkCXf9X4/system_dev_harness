# Simple Project Fixture

This fixture is a minimal development repo used by the pytest-based OpenCode smoke tests.
It gives the workflow a predictable project root without pulling in unrelated files.
